import { TemperatureSymbolPipe } from './temperature-symbol.pipe';

describe('TemperatureSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperatureSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
